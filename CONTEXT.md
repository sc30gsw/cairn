# 学習ログ

本番（TOEIC）に向けて、1人の学習者がその日の学習を残し、履歴と目標を見るための記録。正式な記録は本アプリにあり、Notion はログイン用の身元確認にだけ使う。模試のスコア・分析、リラックス工程、JSON/CSV エクスポートはこのアプリの対象外。

## Language

**日**:
JST の暦日1日分。コンディション・メモ・睡眠と、その日の学習行を所有する。今日と過去は編集できる。未来の日は開けるが行は作らない。行もコンディションもメモも睡眠もない暦日は、日としては存在せず、学習量0の休養と同じ。
_Avoid_: 日次ログ, 学習記録（親行）, Daily log, 未記録と休養を別概念にすること

**行**:
ある日に紐づく学習1件。項目・内容・分数と状態（確定 / 未着手 / スキップ）を持つ。学習量に入るのは確定だけ。プリセットにない行も、その日に追加できる。
_Avoid_: サブアイテム, 子行, エントリ, Entry

**ゴミ箱**:
削除した行と日を戻せる場所。30日後に完全削除する。項目・プリセット・障害プランの削除は即時。使っている行がある項目は消せない。
_Avoid_: 数秒の Undo だけ, 期限なしの永久保管

**項目**:
行が指す学習の種類。カテゴリを1つ持つ。所有者は追加・改名・削除できる。初期データは Notion の種類（模試を除く）。行は項目を1つだけ持つ。
_Avoid_: 教材名をカテゴリと混用すること, 複数選択のタグ, 模試, コードに焼き付けた列挙

**カテゴリ**:
共有文の見出し。TOEIC対策 / 多聴 / 多読 / 英会話 / その他。項目がどれに属するかで決まる。
_Avoid_: 行ごとにカテゴリを選ぶこと

**プリセット**:
日を開いたときに並ぶ行の雛形。どの曜日に使うかを持ち、今日の曜日に合うものが自動適用される。各曜日はプリセット1つだけ。今日だけ別プリセットに切り替えると、未着手の行だけ差し替わり、確定とスキップは残る。所有者は作成・編集・削除できる。
_Avoid_: 曜日タイプをコードに焼き付けた列挙, Notion のテンプレート, 同じ曜日に複数プリセットを重ねること

**睡眠**:
起床した暦日に属する、直前の夜の就寝からその朝の起床まで。夜は日付を選ばず「今夜」に就寝だけ打つ。7時間未満なら警告するが、行の確定は止めない。
_Avoid_: 就寝した暦日への帰属, 就寝と起床を別の日にばらして手で対応づけること, 警告を入力禁止にすること

**今夜**:
まだ終わっていない睡眠。日付を持たない。就寝時刻だけを受け取る。
_Avoid_: 明日の日を開いて就寝を入れること

**コンディション**:
その日の調子。好調 / 普通 / 崩れた の3つ。
_Avoid_: スコア化, リラックスと混ぜること

**履歴**:
入口は月のカレンダー。空のマスは休養。週は Agenda（行をその日の終日として並べる）。日を開くとその日の行と、Slack 共有用の Markdown。アプリの起動は今日の記録。
_Avoid_: 日付降順の一覧を主画面にすること, Markdown を正本にすること, 会議室型のリソースビュー

**共有文**:
確定した行から作る Markdown。カテゴリが1つなら平坦、2つ以上なら親+子。カテゴリは固定順、カテゴリ内は入力順。クリップボードへコピーし、Slack に貼るためのもの。
_Avoid_: アプリ内の記録の正本, Notion の数式プロパティ

**学習量**:
その暦日に確定した行の分数合計。記録のない日は0分。7日移動平均はこの0を含めた暦日で出す。
_Avoid_: 記録した日だけでの平均を「移動平均」と呼ぶこと

**本番目標**:
期限つきの成果。1件だけ持つ。初期値はスコア 730〜850、本番日 2026-09-27。あとから編集できる。アプリ内のフィードバックには使わず、カウントダウンの軸になる。
_Avoid_: 模試スコアの系列をアプリに入れること, OKR の Key Result ツリー, 本番目標を複数持つこと

**週間ゴール**:
月曜始まりの週に対する、学習量の具体的で難しいプロセス目標。目標は分数1つ。履歴がフィードバックになる。
_Avoid_: 毎日の成果ノルマ, 模試点を週間ゴールにすること, 項目ごとの週間ノルマ

**障害プラン**:
「もしこの障害なら、こうする」の if-then。所有者は追加・編集・削除できる。表示と編集のためのデータであり、行の状態を自動では変えない。
_Avoid_: WOOP の想像ステップを画面にすること, 目標の階層ツリー, コンディションから行を自動スキップすること

**所有者**:
このアプリに入れる唯一のアカウント。公開 URL でも、signup は閉じ、email allowlist 以外は入れない。
_Avoid_: ユーザー（複数いる前提）, メンバー, テナント
