# 学習ログ

本番（TOEIC）に向けて、1人の学習者がその日の学習を残し、履歴と目標を見るための記録。正式な記録は本アプリにあり、Notion はログイン用の身元確認にだけ使う。模試のスコア・分析、リラックス工程、JSON/CSV エクスポートはこのアプリの対象外。

## Language

**日**:
JST の暦日1日分。コンディション・メモと、その日の学習の記録を所有する。今日と過去は編集できる。未来の日は開けるが記録は作らない。記録もコンディションもメモもない暦日は、日としては存在せず、学習量0の休養と同じ。
_Avoid_: 日次ログ, 学習記録（日全体）, Daily log, 未記録と休養を別概念にすること

**記録**:
ある日に紐づく学習1件。項目・具体的手順・分数と状態（確定 / 未着手 / スキップ）を持つ。学習量に入るのは確定だけ。プリセットにない記録も、その日に追加できる。具体的手順は「〜を勉強する」ではなく、最初の一歩を書く。
_Avoid_: 行, サブアイテム, 子行, エントリ, Entry, 内容（抽象ラベル）

**具体的手順**:
記録・プリセット行・障害プランの then が持つ実行可能な一歩。項目は学習の種類、具体的手順はその日その場で何をどうするか。確定・保存時は空・空白のみを許さない（1文字以上）。未着手の記録は空を許す。
_Avoid_: 内容, 学習内容, 抽象目標（「英語を勉強する」だけ）, 主語・動詞・目的語の3フィールド分割

**ゴミ箱**:
削除した記録と日を戻せる場所。30日後に完全削除する。項目・プリセット・障害プランの削除は即時。使っている記録がある項目は消せない。
_Avoid_: 数秒の Undo だけ, 期限なしの永久保管

**項目**:
記録が指す学習の種類。カテゴリを1つ持つ。所有者は追加・改名・削除できる。初期データは Notion の種類（模試を除く）。記録は項目を1つだけ持つ。項目画面ではカテゴリの下の小項目として並ぶ。
_Avoid_: 教材名をカテゴリと混用すること, 複数選択のタグ, 模試, コードに焼き付けた列挙

**カテゴリ**:
共有文の見出し。項目がどれに属するかで決まる。所有者は追加・改名・削除できる。初期値は TOEIC対策 / 多聴 / 多読 / 英会話 / その他。項目画面では大項目として並ぶ。
_Avoid_: 記録ごとにカテゴリを選ぶこと, コードに焼き付けた列挙, 大項目を項目と別概念にすること

**プリセット**:
日を開いたときに並ぶ記録の雛形。どの曜日に使うかを持ち、今日の曜日に合うものが自動適用される。各曜日はプリセット1つだけ。今日だけ別プリセットに切り替えると、未着手の記録だけ差し替わり、確定とスキップは残る。所有者は作成・編集・削除できる。
_Avoid_: 曜日タイプをコードに焼き付けた列挙, Notion のテンプレート, 同じ曜日に複数プリセットを重ねること

**履歴**:
入口は月のカレンダー。空のマスは休養。各日にはその日の学習時間と、7日間の1日平均（均）を示す。週は Agenda で、今日を含む月曜始まりの週の記録を並べる（カレンダーの表示月とは独立）。分析では、選んだ日・週・月についてカテゴリ別の内訳と、週の日別ペースを見る。数値の一覧も併記する。日を開くとその日の記録と、Slack 共有用の Markdown。アプリの起動は今日の日。
_Avoid_: 日付降順の一覧を主画面にすること, Markdown を正本にすること, 会議室型のリソースビュー, 模試分析のような複雑ダッシュボード, チャートだけで数値を伝えること

**コンディション**:
その日の調子。好調 / 普通 / 崩れた の3つ。選ぶまで値はない。一度選んだら外せない。
_Avoid_: スコア化, リラックスと混ぜること, 未設定

**共有文**:
確定した記録から作る Markdown。カテゴリが1つなら平坦、2つ以上なら親+子。カテゴリは固定順、カテゴリ内は入力順。クリップボードへコピーし、Slack に貼るためのもの。
_Avoid_: アプリ内の記録の正本, Notion の数式プロパティ

**学習量**:
その暦日に確定した記録の分数合計。記録のない日は0分。7日移動平均はこの0を含めた暦日で出す。
_Avoid_: 記録した日だけでの平均を「移動平均」と呼ぶこと

**目標**:
所有者が達成したい状態。目標タイプを1つと、目標の内容（具体的手順と同じ規則: 空・空白のみ不可）を持つ。試験は1件だけ、ペースは有効1件だけ、達成量・習得・その他は複数持てる。
_Avoid_: OKR の Key Result ツリー, 目標同士の親子関係, 着手日・完了日つきのマイルストーン層

**目標タイプ**:
目標の型。試験 / ペース / 達成量 / 習得 / その他 の固定5値。型が決まると、目標に入力する欄が決まる。目標は型を1つだけ持ち、あとから変えない。記録側のカテゴリとは別概念。
_Avoid_: ジャンル, カテゴリとの混用, 所有者が自由に追加できる分類タグ, 内容領域（学業・健康など）で型を切ること

**本番目標**:
試験タイプの目標。期限つきの成果で、本番日とスコア下限〜上限（TOEIC: 10〜990、5点刻み、下限≤上限）を持つ。1件だけ持つ。アプリ内のフィードバックには使わず、カウントダウンの軸になる。ペース未設定のままの本番目標は未完成として扱い、設定を促す。
_Avoid_: 模試スコアの系列をアプリに入れること, 本番目標を複数持つこと, 本番目標だけで運用すること

**週間ゴール**:
月曜始まりの週に対する頻度のプロセス目標。「週 n 日、1日あたり最低 m 分」で判定し、実施日の数が目標日数に達したら達成。総分数は表示のみで判定に使わない。週の目標値はペース目標から週の開始時に写され、その週だけ変えられる。1週の未達では連続達成を切らず、2週連続の未達で切る。
_Avoid_: 週の合計分数だけで達成判定すること, 毎日の成果ノルマ, 模試点を週間ゴールにすること, 項目ごとの週間ノルマ, 習慣ゴールを別概念にすること

**週間ターゲット**:
カテゴリに対する「何をどれくらい」の常設ノルマ。カテゴリ1つにつき1件だけ。量は 分 / 実施日 / 確定件数 のいずれかで、月曜始まりの今週の確定記録から自動集計する（手入力の進捗申告はしない）。達成は各ターゲットが個別に持ち、週間ゴールの達成・連続達成には影響しない。今週専用の計器であり、過去週には表示しない。
_Avoid_: 項目ごとの週間ノルマ, 手入力の進捗申告, ストリーク判定に混ぜること, 同一カテゴリへの複数ターゲット, 週ごとのスナップショット保存

**実施日**:
確定した記録の合計分数が、その週の「1日あたり最低分数」に達した暦日。週間ゴールの達成判定の単位。
_Avoid_: 確定記録が1件でもあれば実施日と数えること（最低分数未満は数えない）

**障害プラン**:
「もしこの障害なら、こうする」の if-then。then は具体的手順と同じルール（空・空白のみ不可、1文字以上の実行可能な一歩）。所有者は追加・編集・削除できる。表示と編集のためのデータであり、記録の状態を自動では変えない。
_Avoid_: WOOP の想像ステップを画面にすること, 目標の階層ツリー, コンディションから記録を自動スキップすること

**所有者**:
このアプリに入れる唯一のアカウント。公開 URL でも、signup は閉じ、email allowlist 以外は入れない。
_Avoid_: ユーザー（複数いる前提）, メンバー, テナント
