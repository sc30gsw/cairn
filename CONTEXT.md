# 学習ログ

本番（TOEIC）に向けて、1人の学習者がその日の学習を残し、履歴と目標を見るための記録。正式な記録は本アプリにあり、ログインはアカウント（email / username / password）または Notion OAuth で行う。模試のスコア・分析、リラックス工程、JSON/CSV エクスポートはこのアプリの対象外。

## Language

**日**:
JST の暦日1日分。コンディション・メモと、その日の学習の記録を所有する。今日と過去は編集できる。未来の暦日には日を作らない。記録もコンディションもメモもない過去の暦日は、日としては存在せず、休養と同じ。「今日」は JST 0:00 に切り替わり、開きっぱなしのタブでも復帰時に追従する。
_Avoid_: 日次ログ, 学習記録（日全体）, Daily log, 過去の空を未記録と呼ぶこと, 見ただけで過去の休養を日にすること

**学習日**:
日画面の見出し。指しているものは日。ナビのラベルは日のまま。
_Avoid_: ナビや語彙の日を置き換えること, 学習量との混用, Study day

**休養**:
過去で日が無いこと。学習量は0。ボタンで付ける状態ではない。
_Avoid_: 未来の空を休養と呼ぶこと, 記録ゼロの日ドキュメントを休養の印にすること, 今週のまだ来ていない日を休養に数えること

**未記録**:
まだ来ていない暦日。日は無く、休養でもない。
_Avoid_: 過去の空を未記録と呼ぶこと, 未着手の記録がある日を未記録と呼ぶこと

**実行ボード**:
今日の記録の状態（未着手 / 進行中 / 確定 / スキップ）と、チェックポイント（障害プランの then と選択した学習日基準の直近チェックポイント）を見る画面。カンバンは学習日ピッカーと前後日ナビで日を選べる。書く場所は日のまま。日ページと相互リンクする（カード単位のリンクは持たない）。移動は確認を挟まず即反映し、学習量が変わる操作だけ Toast で知らせる。分数が無い記録を確定へ動かすときだけ、その場で入力を求める。
_Avoid_: 移動のたびに確認モーダルを挟むこと, カードから他ページへ飛ぶリンクを置くこと, 開始・終了時刻を記録に足すこと, 日の画面を置き換えること, カンバンだけ常に今日固定にすること, モバイルでドラッグを必須にすること

**進行中**:
記録が取り組み中である状態。実行ボード（カンバン）で直接操作する。日ページでは明示しない。
_Avoid_: 日ページに進行中ボタンを置くこと, 進行中を未着手と同じ UI に潰すこと, 計測中を別の状態として数えること

**計測**:
進行中の記録が測っている経過時間。開始時刻はサーバが持ち、経過は画面が導出する。計測した時間は確定
のときに分数の初期値になり、そこで手直しできる。分数の正は記録の分数のままで、計測は学習量の別系統
ではない。同時に計測するのは1件だけ。1区間が240分に達すると自動停止する。
_Avoid_: 計測を学習量の別系統にすること, 計測値を自動で確定すること, 画面を見ていた時間だけ数えること, 計測なしの進行中を禁止すること, 記録の状態に「計測中」を足すこと

**自動停止**:
1区間が240分を超えた計測を、進行中のまま止めること。放置の後始末であり、確定でも見送りでもない。
分数は自分で直してから確定する。
_Avoid_: 自動で確定すること, 自動で未着手に戻して計測を捨てること, 日付が変わったら止めること

**記録**:
ある日に紐づく学習1件。項目・ひとこと・分数と状態（確定 / 未着手 / 進行中 / スキップ）を持つ。学習量に入るのは確定だけ。プリセットにない記録も、その日に追加できる。ひとことは空でよい — 毎回同じ項目で書くことがない日はそのままにする。日ページでは見送り・見送り取消に Confirm を出す。カンバンではドラッグで即反映する。
_Avoid_: 行, サブアイテム, 子行, エントリ, Entry, 内容（抽象ラベル）, ひとこと空を確定の妨げにすること, 見送りを確定の解除と同じ操作にすること, オフラインで保存できたように見せること

**具体的手順**:
目標の内容・障害プランの then が持つ実行可能な一歩。「〜を勉強する」ではなく、最初の一歩を書く。空・空白のみは許さない（1文字以上）。記録・プリセット行の同じ位置のフィールドはひとことと呼び、別の規則を持つ。
_Avoid_: 内容, 学習内容, 抽象目標（「英語を勉強する」だけ）, 主語・動詞・目的語の3フィールド分割, 記録・プリセット行をこの語で呼ぶこと

**ひとこと**:
記録・プリセット行が持つ、項目名だけでは足りないときの一行の補足。空を許す（1文字も不要）。模試の点数（例: 模試 1（28 / 30））、教材の単元（例: Chapter 3）など、実行前に書くことも確定後に書くこともできる。プリセット行のひとことは、そのまま記録のひとことになる。確定ゲートはこの項目にかからない。
_Avoid_: 具体的手順と同じ規則を適用すること, 内容, 学習内容, 空を拒否すること

**ホーム画面アプリ**:
ホーム画面に追加して、ブラウザの枠なしで起動する形。ログインの保持が長くなり、通知の前提にもなる。
オフラインでは記録できない — 圏外ではオフライン貼り紙が出るだけで、記録も履歴も出ない。
_Avoid_: オフラインで記録できると読める案内, 8番目のナビタブ, 自動で出るインストール催促, オフライン時点の写しを記録として見せること

**オフライン貼り紙**:
圏外でアプリを開いたときに出る、紙1枚だけの画面。オフラインでは記録できないことを書き、
電波が戻ると自動で元の画面へ戻る。データは出さない。
_Avoid_: 前回のデータを載せること, 再ログインを要求すること, これを日の代わりにすること

**ゴミ箱**:
削除した記録と日を戻せる場所。30日後に完全削除する。項目・プリセット・障害プランの削除は即時。使っている記録がある項目は消せない。
_Avoid_: 数秒の Undo だけ, 期限なしの永久保管

**項目**:
記録が指す学習の種類。カテゴリを1つ持つ。所有者は追加・改名・削除できる。初期データは Notion の種類（模試を除く）。記録は項目を1つだけ持つ。項目画面ではカテゴリの下の小項目として並ぶ。
_Avoid_: 教材名をカテゴリと混用すること, 複数選択のタグ, 模試, コードに焼き付けた列挙

**カテゴリ**:
共有文の見出し。項目がどれに属するかで決まる。所有者は追加・改名・削除できる。初期値は TOEIC対策 / 多聴 / 多読 / 英会話 / その他。項目画面では大項目として並ぶ。
_Avoid_: 記録ごとにカテゴリを選ぶこと, コードに焼き付けた列挙, 大項目を項目と別概念にすること

**プリセット**:
記録の雛形。どの曜日に使うかを持ち、各曜日は1つだけ。今日を開くと、その曜日に合うものが自動で並ぶ。編集できる日（今日と過去）で別プリセットに切り替えると、未着手だけ差し替わり、確定とスキップは残る。所有者は作成・編集・削除できる。プリセット行のひとことは空でよい。
_Avoid_: 曜日タイプをコードに焼き付けた列挙, Notion のテンプレート, 同じ曜日に複数プリセットを重ねること, 今日以外では切り替えられないことにすること

**履歴**:
入口は月のカレンダー。過去の空マスは休養。まだ来ていないマスは未記録。各日にはその日の学習時間と、7日間の1日平均（均）を示す。月マスと週 Agenda には、選んだコンディションも出す。週は Agenda で、今日を含む月曜始まりの週の記録を並べる（カレンダーの表示月とは独立）。分析では、選んだ日・週・月についてカテゴリ別の内訳と、週の日別ペースを見る。同じ範囲でコンディション別の学習量も見る。数値の一覧も併記する。同じ分析に、今日を除く直近28暦日の曜日ごとの消化を置く。消化が低い曜日には、プリセット画面へのリンクを出す。日を開くとその日の記録と、Slack 共有用の Markdown。アプリの起動は今日の日。
_Avoid_: 日付降順の一覧を主画面にすること, Markdown を正本にすること, 会議室型のリソースビュー, 模試分析のような複雑ダッシュボード, チャートだけで数値を伝えること, 未来の空を休養として集計すること, 休養を計画倒れに数えること, 今日の未着手を計画倒れに数えること, 消化を分析の日・週・月ピッカーに追従させること, 消化の定義をコンディション別学習量で置き換えること

**消化**:
ある曜日に並んだ記録のうち、確定した件数の割合（確定 / 並んだ件数）。プリセット計画が残ったかどうかの指標であり、学習量でも週間ターゲットの達成でもない。
_Avoid_: 達成率, 項目ごとの消化, 分数の予実比, 項目別の週間ノルマ

**コンディション**:
その日の調子。好調 / 普通 / 崩れた の3つ。選ぶまで値はない。一度選んだら外せない。
_Avoid_: スコア化, リラックスと混ぜること, 未設定

**共有文**:
確定した記録から作る Markdown。カテゴリの見出しは常に出す（カテゴリが1つでも省略しない）。カテゴリ内の記録がちょうど1件で、ひとことが空で、項目名がカテゴリ名と一致するときだけ、見出しと記録を1行に畳む。カテゴリは固定順、カテゴリ内は入力順。カテゴリ間に空行は入れない。クリップボードへコピーし、Slack に貼るためのもの。
_Avoid_: アプリ内の記録の正本, Notion の数式プロパティ, カテゴリが1つのときだけ平坦にすること

**学習量**:
その暦日に確定した記録の分数合計。記録のない日は0分。7日移動平均はこの0を含めた暦日で出す。
_Avoid_: 記録した日だけでの平均を「移動平均」と呼ぶこと

**目標**:
所有者が達成したい状態。目標タイプを1つと、目標の内容（具体的手順と同じ規則: 空・空白のみ不可）を持つ。試験は1件だけ、習得は複数持てる。習得のうちチェックポイントだけが必須の親（本番目標 or 長期目標）を持つ限定的な階層（最大2層）を持つ。それ以外の目標に親子はない。
_Avoid_: OKR の Key Result ツリー（任意の深さで全目標を親子にすること）, チェックポイントの親子を超えて階層を広げること, 着手日・完了日つきのマイルストーン層

**目標タイプ**:
目標の型。試験 / 習得 の固定2値。判別子は判定の入り方で、試験は外部の期限日に一度だけ結果が入り、習得は自己判定でいつでも達成にできる。型が決まると、目標に入力する欄が決まる。目標は型を1つだけ持ち、あとから変えない。記録側のカテゴリとは別概念。
_Avoid_: ジャンル, カテゴリとの混用, 所有者が自由に追加できる分類タグ, 内容領域（学業・健康など）で型を切ること, 期限の有無だけで型を増やすこと

**本番目標**:
試験タイプの目標。期限つきの成果で、本番日とスコア下限〜上限（TOEIC: 10〜990、5点刻み、下限≤上限）を持つ。1件だけ持つ。アプリ内のフィードバックには使わず、カウントダウンの軸になる。週間ターゲット未設定のままの本番目標は未完成として扱い、設定を促す。
_Avoid_: 模試スコアの系列をアプリに入れること, 本番目標を複数持つこと, 本番目標だけで運用すること

**習得**:
習得タイプの目標。「〜できる」の到達基準を内容に持ち、達成は所有者の自己判定で、達成日を残す。期限は任意で、既定は次の日曜。達成しても消えず、達成済みの一覧が達成の履歴になる。期限を過ぎても表示が変わるだけで、期限は編集できる。自己判定の較正のため、**対象項目の**学習量の実績を併記する。実績は達成時点で固定され、達成後の学習で書き換わらない（達成を外せば現在進行形に戻る）。期限の有無だけで長期目標とチェックポイントの2区分に分かれる（判別子はこれ以外に増やさない）。期限を付ける／外す操作で区分を双方向に移行できる。
_Avoid_: 学習量からの自動達成判定, 未達の自動失敗記録, ストリーク, 模試スコアでの判定, 期限の有無以外の基準で区分すること

**対象項目**:
習得（長期目標・チェックポイント）が実績に数える記録の範囲。項目の集合で表し、未指定はすべての記録。記録の項目がこの集合にあれば、その確定分数と実施日が実績に入る。記録の側で目標を選ぶことはしない。達成済みの目標では実績が凍結されているので変更できない（達成を外せば変更できる）。対象項目にしている項目は削除できない。
_Avoid_: 記録ごとに目標を選ぶこと, カテゴリ単位の指定, 進捗の手入力申告, 達成率・進捗バーの分母, 対象外の記録を未達に数えること

**長期目標**:
期限を持たない習得の呼び名。トップ層に位置し、本番目標からは独立して作成できる（新規フォームに期限欄を出さない）。他の目標の子にはならない。期限を付けて親（本番目標または別の長期目標）を選ぶとチェックポイントに変わる。子チェックポイントを持つ長期目標は、達成済みの子しか残っていなくても自身をチェックポイント化できない（階層は最大2層に固定）。
_Avoid_: マイルストーン, 期限を持つ習得と同一視すること, 本番目標の子として作ること, 件数上限を設けること

**チェックポイント**:
期限を持つ習得の呼び名。必須の親（本番目標または長期目標）を FK で持ち、親の下に期限順で並んで「いつまでに何ができているか」を刻む。親ごとに同時に持つのは1〜2件の粒度が目安（3件目で助言。この件数は親ごとに数え、長期目標自体の件数には上限がない）。期限を外すと親を失い長期目標に戻る。親を削除すると、その親を持つチェックポイントは同一トランザクションでカスケード削除される（目標にはゴミ箱が無く不可逆）。
_Avoid_: マイルストーン・OKR・Key Result という呼び名, 独立した目標タイプにすること, チェックポイントを親に持つこと（チェーン禁止）, チェックポイントの列化, 親を持たない状態で期限だけ持つこと

**週間ターゲット**:
カテゴリに対する「何をどれくらい」の常設ノルマで、このアプリのプロセス目標の担い手。カテゴリ1つにつき1件だけ。量は 分 / 実施日（確定した記録が1件でもある暦日） / 確定件数 のいずれかで、月曜始まりの今週の確定記録から自動集計する（手入力の進捗申告はしない）。達成は各ターゲットが個別に持つ。今週専用の計器であり、過去週には表示しない。
_Avoid_: 項目ごとの週間ノルマ, 手入力の進捗申告, ストリーク, 同一カテゴリへの複数ターゲット, 週ごとのスナップショット保存, 実施日への最低分数フロア

**障害プラン**:
「もしこの障害なら、こうする」の if-then。then は具体的手順と同じルール（空・空白のみ不可、1文字以上の実行可能な一歩）。所有者は追加・編集・削除できる。表示と編集のためのデータであり、記録の状態を自動では変えない。
_Avoid_: WOOP の想像ステップを画面にすること, 目標の階層ツリー, コンディションから記録を自動スキップすること

**方法カタログ**:
「勉強方法どうやるのが効率なんだっけ」を思い出すために引く、所有者の参照専用の置き場。正本は Convex。独立したナビタブ(/methods)を持ち、項目画面と同じ紙 — レーンの列に方法カードが並び、ドラッグで並べ替える。空で始まり、初期データは入れない。セットアップの完了条件が数える「カタログ」(項目・プリセット)とは別物。
_Avoid_: 目標画面への同居, 初期 seed(3回法・リスニング・リーディング等の教材文), YouTube 要約の持ち込み, セットアップのカタログと混ぜること

**レーン**:
方法カタログの親グループ(例: 模試レーン / 単語レーン)。方法はレーンを1つ持つ。カテゴリ(共有文の見出し。項目が属する)とは別概念で、方法をカテゴリへリンクしない。
_Avoid_: カテゴリとの混用, 方法とカテゴリのリンク, コードに焼き付けた列挙

**方法**:
レーンに属する学習のやり方1件。開いたカードはタイトル・本文(textarea)・完了条件(タスクリスト)・メモ(リンク可)の4欄だけ。見て思い出すためのものであり、記録・未着手・プリセット・目標のどれにも触れない(参照専用)。具体的手順(目標・障害プランの一歩)ともひとこと(記録の補足)とも別の概念。削除は即時(項目・プリセット・障害プランと同じ)。
_Avoid_: やり方 / 手順書 / 勉強法という呼び名, 記録・項目・プリセット・目標への FK や自動連動, 本文・完了条件・メモへの確定ゲート

**いま見る**:
方法カタログの正面に置く1件の印。所有者ごとに高々1件で、別の方法に立てると前の印は外れる(0件も正常)。今日のボードには何も起こさない。
_Avoid_: 進行中, 採用中, 今日の記録・未着手を変えること, 複数のいま見る

**所有者**:
このアプリに入れる唯一のアカウント。公開 URL でも、signup は閉じ、email allowlist 以外は入れない。
_Avoid_: ユーザー（複数いる前提）, メンバー, テナント

**マイページ**:
アカウントメニューから開く `/my-page`。プロフィール（256px アバター・表示名・@username）、アカウント編集（表示名・ユーザー名・パスワード）、パスキー、今日の状況（本番目標があるときのみ）、セットアップ checklist、カタログ例（閲覧専用）を置く。メール変更は v1 対象外。
_Avoid_: 8番目のナビタブ, カタログの自動 seed, メール変更 UI

**セットアップ**:
新規ユーザーは空のカタログから始める。完了条件は 項目≥1・プリセット≥1・本番目標・週間ターゲット≥1。ホームの Stepper とマイページ checklist の両方から同じ状態を見る。ステップの「あとで設定」は localStorage に記録する。
_Avoid_: 初回ログイン時の自動 seed, EmptyCatalogBanner の復活
