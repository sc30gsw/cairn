# dependencies
node_modules
.pnpm-store

# output
dist
.output

# generated tool dirs
.tanstack

# Service Worker の中間バンドル出力(scripts/build-sw.ts)
.pwa

# environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.cache
.fallow
*.tsbuildinfo

# `vp test --coverage` の出力。再生成できるのでコミットしない。
coverage

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
